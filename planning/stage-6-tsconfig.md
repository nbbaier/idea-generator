### Stage 6: TSConfig modernization

Todos:

-  [ ] In `tsconfig.json`, set `target` to `es2022` (Next will transpile as needed).
-  [ ] Remove `allowJs` if there are no `.js` sources in `src/`.
-  [ ] Keep `strict: true`; ensure no type regressions.


